/* 문자값 */

/* 문법
 * ' 문자 ' 형태로 나타냅니다.
 * 문자에는 ', \, 엔터를 제외한 문자들을 쓸 수도 있고,  
 * 특수문자 문법에 따라서 쓸 수도 있습니다.
 *
 * 의미
 * 컴파일러에서 정해진대로 문자에 해당하는 정수값이 됩니다. 
 */

int i = 0; /* To Do: i의 초기값을 문자 'a'에 해당하는 정수값으로 설정해보세요 */
int j = 0; /* To Do: j의 초기값을 문자 'Z'에 해당하는 정수값으로 설정해보세요 */
int k = 0; /* To Do: k의 초기값을 문자 '0'에 해당하는 정수값으로 설정해보세요 */

int main () {
   return 0;
}
