/* 식문 */

/* 문법
 * 식 ; 형식으로 식 문을 씁니다.
 * 식은 값, 식 + 식, 함수호출식, 변수 등이 있으며, 생략가능합니다. 
 *
 * 의미
 * 식을 계산하고 계산 결과는 쓰지 않습니다. 
 * 식을 생략한 경우 (세미콜론만 있을 경우)는 어떤 작업도 하지 않습니다.   
 */
#include <stdio.h>
int main () {
   /* To Do: printf 함수를 호출해서 화면에 Hello라는 문자열을 출력해보세요 */
   return 0;
}
